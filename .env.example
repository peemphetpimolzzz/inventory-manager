# Copy to .env before running `docker compose up`.
# SQL Server requires a strong password: >= 8 chars, with upper, lower, digit, and symbol.
SA_PASSWORD=Your_strong_Pass123!

# Host ports (DB_PORT off 1433 to avoid clashing with a local SQL Server)
WEB_PORT=8080
API_PORT=8081
DB_PORT=14330
